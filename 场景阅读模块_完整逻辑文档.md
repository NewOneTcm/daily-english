# 「场景阅读」模块 完整逻辑文档

> 用途：本文档可直接作为 AI 生成代码的需求说明书。所有字段名、公式、阈值、接口、Prompt 均已给出，实现时无需再自行设计。

---

## 0. 模块定位与术语

| 项 | 内容 |
|---|---|
| 模块名 | 场景阅读（Context Practice） |
| 宿主 | 独立英语学习 Web 应用 |
| 生词来源 | 阅读模块中用户标记并加入生词本的词 |
| 核心目标 | 让"不熟的词"在真实语境中反复出现，通过场景线索猜词义 → 巩固记忆 |
| 一句话逻辑 | **取不熟次数最高的 10 个词 → 归到同一场景 → 生成 500 字短文 → 文中高亮 → 用户标记「还不熟 / 认识了」→ 回写计数 → 越不熟的词出现频率越高** |

**关键设计决策（与初版思路的差异，请严格按此实现）**

1. 计数分三个字段，不再合并：`unknown_count`（不熟次数，核心）、`exposure_count`（曝光篇数，辅助）、`known_streak`（连续认识次数，用于毕业）。
2. 从阅读模块加入生词本时，`unknown_count` 初始值 = **1**（加入动作即"我不认识"），不是 0。
3. 待练池筛选条件：`status = 'learning' AND unknown_count >= 1`。
4. 排序权重以 `unknown_count` 为绝对主项，其余字段仅做微调（防重复轰炸、间隔重复）。

---

## 1. 数据模型

### 1.1 SQL 表结构

```sql
-- 生词表（与阅读模块共用）
CREATE TABLE words (
  id               TEXT PRIMARY KEY,
  user_id          TEXT    NOT NULL,
  lemma            TEXT    NOT NULL,              -- 原形，如 "go"（短语存完整串，如 "look forward to"）
  surface          TEXT,                          -- 用户在原文中碰到的形式，如 "went"
  pos              TEXT,                          -- 词性：n. / v. / adj. / adv. / phrase
  user_gloss       TEXT,                          -- 用户自己标注的释义
  required_sense   TEXT,                          -- 用户希望练习的义项（一词多义时用）
  is_phrase        INTEGER DEFAULT 0,             -- 是否短语/词组

  source           TEXT DEFAULT 'reading',        -- reading | manual | import
  source_context   TEXT,                          -- 阅读模块中的原句（文章中点击单词时要回显）
  source_doc_id    TEXT,                          -- 来源文章 ID

  unknown_count    INTEGER DEFAULT 1,             -- ★ 不熟次数（核心排序字段）
  exposure_count   INTEGER DEFAULT 0,             -- 进入过几篇场景文章
  known_streak     INTEGER DEFAULT 0,             -- 连续标记「认识了」的次数

  status           TEXT DEFAULT 'learning',       -- learning | mastered
  last_exposed_at  DATETIME,
  last_unknown_at  DATETIME,
  mastered_at      DATETIME,
  created_at       DATETIME,
  updated_at       DATETIME
);
CREATE UNIQUE INDEX idx_words_user_lemma ON words(user_id, lemma, required_sense);
CREATE INDEX idx_words_pool ON words(user_id, status, unknown_count DESC);

-- 场景文章表
CREATE TABLE context_articles (
  id            TEXT PRIMARY KEY,
  user_id       TEXT NOT NULL,
  title         TEXT,
  title_zh      TEXT,
  content_en    TEXT,
  content_zh    TEXT,
  genre         TEXT,                             -- story | news | dialogue | diary | essay
  level         TEXT,                             -- A2 | B1 | B2 | C1
  scene         TEXT,                             -- 本篇构思的场景描述
  word_count    INTEGER,
  params_json   TEXT,                             -- 生成参数快照（用于缓存去重）
  prompt_hash   TEXT,                             -- 参数+词表 hash，命中则复用缓存
  status        TEXT DEFAULT 'draft',             -- draft | finished
  created_at    DATETIME,
  finished_at   DATETIME
);

-- 文章 × 目标词 关联表（记录本篇中每个目标词的表现）
CREATE TABLE context_article_words (
  id              TEXT PRIMARY KEY,
  article_id      TEXT NOT NULL,
  word_id         TEXT NOT NULL,
  matched_forms   TEXT,                           -- JSON 数组：文中实际出现的形式，如 ["negotiations"]
  first_sentence  TEXT,                           -- 首次出现的完整英文句子
  clue_type       TEXT,                           -- 同义复现 / 反义对比 / 举例 / 定义解释 / 因果情境
  sense_zh        TEXT,                            -- 文中义
  explanation_zh  TEXT,                            -- 线索说明
  marked_result   TEXT,                            -- NULL 未标记 | unknown | known
  marked_at       DATETIME
);
CREATE INDEX idx_caw_article ON context_article_words(article_id);

-- 事件流水（可选，用于回溯与可视化）
CREATE TABLE word_events (
  id          TEXT PRIMARY KEY,
  word_id     TEXT NOT NULL,
  event_type  TEXT NOT NULL,                      -- add | expose | unknown | known | master
  article_id  TEXT,
  created_at  DATETIME
);
```

### 1.2 TypeScript 类型

```ts
type WordStatus = 'learning' | 'mastered';
type MarkResult = 'unknown' | 'known';
type Genre = 'story' | 'news' | 'dialogue' | 'diary' | 'essay';
type Level = 'A2' | 'B1' | 'B2' | 'C1';

interface Word {
  id: string;
  userId: string;
  lemma: string;
  surface?: string;
  pos?: string;
  userGloss?: string;
  requiredSense?: string;
  isPhrase: boolean;
  source: 'reading' | 'manual' | 'import';
  sourceContext?: string;
  sourceDocId?: string;
  unknownCount: number;     // 初始 1
  exposureCount: number;
  knownStreak: number;
  status: WordStatus;
  lastExposedAt?: Date;
  lastUnknownAt?: Date;
  masteredAt?: Date;
  createdAt: Date;
  updatedAt: Date;
}

interface TargetWordInArticle {
  word: string;
  matchedForms: string[];
  firstSentence: string;
  clueType: string;
  senseZh: string;
  explanationZh: string;
}

interface ContextArticle {
  id: string;
  userId: string;
  title: string;
  titleZh: string;
  contentEn: string;
  contentZh: string;
  genre: Genre;
  level: Level;
  scene: string;
  wordCount: number;
  targetWords: TargetWordInArticle[];
  status: 'draft' | 'finished';
  createdAt: Date;
  finishedAt?: Date;
}
```

---

## 2. 状态机

```
（阅读模块中标记生词，加入生词本）
        ↓
   words 记录创建：unknown_count = 1, status = 'learning'
        ↓
┌────────────────── learning（待练池成员）──────────────────┐
│                                                            │
│  进入场景文章并点击「还不熟」 ──→ unknown_count += 1        │
│                                  known_streak = 0          │
│                                  （回到池顶，优先再练）     │
│                                                            │
│  进入场景文章并点击「认识了」 ──→ known_streak += 1         │
│                                  unknown_count 不变        │
│                                                            │
│          known_streak >= 3  ──→  status = 'mastered'       │
└────────────────────────────────────────────────────────────┘
        ↓
   mastered（移出待练池，不再参与选词）
        ↓
   V2：每月随机抽 5% 已掌握词回炉检验，若再次标记「还不熟」
       → status = 'learning', unknown_count += 1, known_streak = 0
```

**关键规则**

- `unknown_count` **只增不减**，它是"这个词有多难"的长期指标，也是排序主项。
- `known_streak` 只在标记「还不熟」时清零，否则累加。
- 毕业阈值 `MASTERED_STREAK = 3`（可配置）。
- 已 `mastered` 的词不再进选词池，避免无限重复。

---

## 3. 选词算法

### 3.1 待练池过滤（硬条件，必须全部满足）

```sql
SELECT * FROM words
WHERE user_id = :uid
  AND status = 'learning'
  AND unknown_count >= 1
ORDER BY priority_score DESC
LIMIT 30;
```

### 3.2 优先级打分公式

```ts
const CONFIG = {
  W_UNKNOWN:      10,   // 不熟次数权重（核心）
  W_NEW:          50,   // 从未曝光过的加成
  W_DAYS:        1.5,   // 距上次曝光天数权重（间隔重复）
  W_STREAK_PEN:   20,   // 快掌握的降权
  W_RECENT_PEN:   40,   // 最近 3 天刚出现过的惩罚
  RECENT_DAYS:     3,
  PICK_COUNT:     10,   // 目标取词数
  MIN_PICK_COUNT:  5,   // 最少取词数
};

function priorityScore(w: Word, now: Date): number {
  const daysSince = w.lastExposedAt
    ? (now.getTime() - w.lastExposedAt.getTime()) / 86400000
    : 999;                                    // 从未曝光视为极久

  return (
    w.unknownCount * CONFIG.W_UNKNOWN
    + (w.exposureCount === 0 ? CONFIG.W_NEW : 0)
    + Math.min(daysSince, 60) * CONFIG.W_DAYS
    - w.knownStreak * CONFIG.W_STREAK_PEN
    - (daysSince < CONFIG.RECENT_DAYS ? CONFIG.W_RECENT_PEN : 0)
  );
}
```

**语义说明**：一个被标记 5 次不熟的词，得分基础 50，会稳定压过标记 1 次的新词（基础 10 + 新词加成 50 = 60）。加成是"一次性"的，不熟次数是"累积性"的——**练得越多越不熟，越会一直出现**，这正是你要的效果。

### 3.3 主题聚类（第二步，不可省略）

10 个互不相关的生词硬塞进 500 字会生成"单词拼盘"，用户读两句就走。必须先做场景归并。

**MVP 方案（推荐，零额外依赖）**：把 Top 30 候选交给 LLM，让它选出能进入同一场景的 10 个。详见 §4.2 Stage 1 Prompt。

**V2 方案（更准）**：用 embedding（`text-embedding-3-small` 等）对候选词向量化，做一次简单聚类，取最大的簇，不足则从最近邻补齐。

**降级规则**：若 LLM 认为无法凑够 10 个词进同一场景，允许返回 5–9 个，**宁可少也不要生硬**。少于 5 个时中止生成，提示用户"可练的词太少，先去阅读模块标记一些生词"。

### 3.4 手动选词（保留入口）

用户可在生词库中手动勾选若干词生成文章。手动模式下：
- 跳过 `priorityScore` 排序，但仍保留 §3.3 主题聚类（或提示用户自行保证主题相关）。
- 手动选词数量限制 3–15 个。
- 手动选词同样走 §5 的计数回写逻辑。

---

## 4. 文章生成

### 4.1 生成参数（默认值）

| 参数 | 默认值 | 可选值 |
|---|---|---|
| 取词数 | 10 | 5–15 |
| 字数 | 500 词 | 300 / 500 / 800 |
| 体裁 | story | story / news / dialogue / diary / essay |
| 难度 | B1 | A2 / B1 / B2 / C1 |
| 输出 | 英文 + 中文对照 | 纯英文 / 英中对照 |
| 场景 | 自动推断 | 自动 / 用户指定 |

### 4.2 Prompt 模板

#### Stage 1：选词与定场景

```
你是英语教学助手。以下是用户标记过"不熟"的生词候选（按优先级从高到低排列）：

{candidates_json}
// 每项格式：{"word":"原形","pos":"词性","user_gloss":"用户标注释义","unknown_count":不熟次数}

请从中挑选 {PICK_COUNT} 个词，要求：
1. 这些词必须能被自然地放进同一个场景或主题（例如：一场职场谈判、一次独自旅行、一段渐行渐远的友情、一次关于城市环保的讨论）；
2. 优先挑选 unknown_count 高的词（越不熟越该出现）；
3. 如果强行凑满数量会导致场景割裂、语句生硬，允许少选（最少 {MIN_PICK_COUNT} 个），并在 reason 中说明原因；
4. 不要造词，不要改变候选词的原形，不要使用候选词表以外的词。

严格输出如下 JSON，不要输出任何多余文字：
{
  "scene": "一句话描述你构思的场景",
  "words": ["word1", "word2"],
  "reason": "这些词为何能放进同一场景"
}
```

#### Stage 2：生成文章

```
你是英语教学助手，请为英语学习者写一篇用于"在语境中巩固生词"的英文短文。

【目标词】以下词必须全部出现在文章中（允许使用它们的常见变形：复数、时态、分词、派生词）：
{target_words_json}
// 每项格式：{"word":"原形","pos":"词性","required_sense":"用户希望练习的义项","user_gloss":"用户标注释义","is_phrase":true/false}

【文章要求】
1. 字数：约 {WORD_COUNT} 个英文单词（允许 ±10%）；
2. 体裁：{GENRE}；场景：{SCENE}；难度：CEFR {LEVEL}；
3. 除目标词外，其余用词和句式必须明显低于目标词难度，确保目标词是全文"最难的部分"，上下文足够易懂；
4. 每个目标词出现 1–3 次，且**首次出现时必须自带语境线索**，让读者不查词典也能猜出词义。可用线索类型（每个词选一种，全文尽量分散使用不同类型）：
   - 同义/近义复现：...a pragmatic approach, that is, a practical way to...
   - 反义对比：Unlike his gregarious brother, Tom was painfully shy.
   - 举例/列举：She felt a surge of emotions—anger, fear, and relief.
   - 定义/解释：The deadline, or the final date to submit, is Friday.
   - 因果/情境：Because the negotiations collapsed, both sides walked away empty-handed.
5. 目标词必须出现在**有信息量的句子**里（承载情节推进或论点支撑），不要放在无关紧要的过渡句中；
6. 短文要有完整情节或清晰论点，读起来像一篇正常文章，不要写成"单词例句拼盘"；
7. 使用常见英文名（Tom / Sarah / Mr. Chen 等），不要用生僻人名地名；
8. 若某个词标有 required_sense，文中必须使用该义项，不得使用其他义项。

【输出格式】严格输出如下 JSON，不要输出任何多余文字：
{
  "title": "英文标题",
  "title_zh": "中文标题",
  "content_en": "英文正文，段落之间用 \\n\\n 分隔",
  "content_zh": "中文全文翻译，段落与 content_en 对应",
  "target_words": [
    {
      "word": "原形",
      "matched_forms": ["文章中实际出现的形式，如 negotiations"],
      "first_sentence": "该词首次出现的完整英文句子",
      "clue_type": "同义复现 / 反义对比 / 举例 / 定义解释 / 因果情境（五选一）",
      "sense_zh": "该词在这句话中的中文意思",
      "explanation_zh": "用中文一句话解释这里的线索如何帮助猜出词义（30 字以内）"
    }
  ],
  "comprehension_questions": [
    { "q": "一道英文阅读理解题", "a": "英文参考答案" }
  ]
}
```

### 4.3 生成后校验（必须实现）

LLM 不保证每个词都出现。生成后必须做词形还原比对：

```ts
interface ValidateResult {
  pass: boolean;
  missing: string[];   // 完全没出现的词
  overused: string[];  // 出现 > 3 次的词
}

function validateArticle(contentEn: string, targets: Word[]): ValidateResult {
  const tokens = tokenize(contentEn);           // 按非字母切分，保留连字符与撇号
  const lemmaSet = buildLemmaSet(tokens);       // 每个 token 还原为若干可能的原形

  const missing: string[] = [];
  const overused: string[] = [];

  for (const t of targets) {
    const count = t.isPhrase
      ? countPhrase(contentEn, t.lemma)         // 短语：完整串匹配
      : countLemma(lemmaSet, t.lemma);          // 单词：原形匹配

    if (count === 0) missing.push(t.lemma);
    else if (count > 3) overused.push(t.lemma);
  }
  return { pass: missing.length === 0 && overused.length === 0, missing, overused };
}
```

**词形还原实现方式**（按推荐顺序）

1. 后端 Node：`compromise` npm 包（纯 JS、无原生依赖、轻量）。
2. 后端 Python：`spaCy`（`en_core_web_sm`）或 `nltk.WordNetLemmatizer`。
3. 兜底：自建候选变形集 + 精简不规则表。

```ts
// 兜底方案：为原形生成常见变形候选集，再与文本 token 求交集
function candidateForms(lemma: string): Set<string> {
  const s = new Set<string>([lemma, lemma.toLowerCase()]);
  const add = (x: string) => s.add(x.toLowerCase());

  add(lemma + 's');  add(lemma + 'es');
  add(lemma.replace(/y$/, 'ies'));                 // study → studies
  add(lemma + 'ed'); add(lemma + 'd');
  add(lemma.replace(/e$/, 'ed'));
  add(lemma.replace(/y$/, 'ied'));                 // study → studied
  add(lemma + 'ing');
  add(lemma.replace(/e$/, 'ing'));                 // negotiate → negotiating
  add(lemma.replace(/([bdgmnprt])$/, '$1$1ing'));  // run → running
  add(lemma + 'ly'); add(lemma.replace(/y$/, 'ily'));
  add(lemma + 'ness'); add(lemma + 'er'); add(lemma + 'ers');
  add(lemma + 'ion'); add(lemma + 'ment'); add(lemma + 'able');
  return s;
}

// 精简不规则表，按需扩充
const IRREGULAR: Record<string, string[]> = {
  go:     ['went', 'gone', 'goes', 'going'],
  get:    ['got', 'gotten', 'gets', 'getting'],
  be:     ['am', 'is', 'are', 'was', 'were', 'been', 'being'],
  have:   ['has', 'had', 'having'],
  do:     ['does', 'did', 'done', 'doing'],
  see:    ['saw', 'seen', 'sees', 'seeing'],
  take:   ['took', 'taken', 'takes', 'taking'],
  come:   ['came', 'comes', 'coming'],
  know:   ['knew', 'known', 'knows', 'knowing'],
  think:  ['thought', 'thinks', 'thinking'],
  bring:  ['brought'],  catch: ['caught'],  teach: ['taught'],
  buy:    ['bought'],   fight: ['fought'],  seek:  ['sought'],
  man:    ['men'],  woman: ['women'],  child: ['children'],
  foot:   ['feet'], tooth: ['teeth'],  mouse: ['mice'],
  datum:  ['data'], medium: ['media'], criterion: ['criteria'],
  analysis: ['analyses'], thesis: ['theses'], basis: ['bases'],
};
```

**短语匹配**：`countPhrase` 需容忍大小写、多余空格、连字符差异：

```ts
function countPhrase(text: string, phrase: string): number {
  const norm = (s: string) => s.toLowerCase().replace(/[\s\-]+/g, ' ').trim();
  const hay = norm(text);
  const needle = norm(phrase);
  let count = 0, i = 0;
  while ((i = hay.indexOf(needle, i)) !== -1) { count++; i += needle.length; }
  return count;
}
```

**重试策略**

```
最多重试 2 次：
  第 1 次重试 → 把 missing 列表写进 user 消息，明确要求补上
  第 2 次重试 → 移除最难匹配的 1–2 个词（unknown_count 最低的），重跑 Stage 1 + Stage 2
  仍失败 → 用剩余词正常出文章，并在 UI 顶部提示"本篇有 N 个词未能自然融入"
```

**缓存**：对 `hash(userId + 词表 + 字数 + 体裁 + 难度)` 做 key 缓存 24 小时，避免用户反复点生成烧 token。

---

## 5. 阅读页交互与计数回写

### 5.1 页面结构

```
┌─────────────────────────────────────────┐
│ 标题（英文 / 中文）                       │
│ 标签：体裁 · 难度 · 500 词 · 10 个目标词   │
├─────────────────────────────────────────┤
│                                          │
│ 正文（目标词高亮，颜色见下）               │
│                                          │
├─────────────────────────────────────────┤
│ 底部浮动条：                              │
│   已标记 3/10    [ 全部认识了 ]  [ 完成 ]  │
└─────────────────────────────────────────┘
```

### 5.2 高亮规则

只渲染待练池中的词，按不熟次数分两档（因为待练池全是"不熟"的词，不再需要三色）：

| 不熟次数 | 样式 | 含义 |
|---|---|---|
| 1–2 次 | 橙色下划线 `border-bottom: 2px solid #f59e0b` | 一般难 |
| ≥3 次 | 红色下划线 + 淡红底 `background: #fef2f2` | 顽固难词，重点看 |
| 已标记「还不熟」 | 下划线加粗 | 本篇当场反馈 |
| 已标记「认识了」 | 下划线变灰、透明度降低 | 本篇已消化的视觉反馈 |

**渲染实现**：不要正则替换 HTML（会破坏标签）。建议把 `content_en` 切成 token 数组，在 React/Vue 中用 `<span>` 包裹目标 token 渲染；短语按 token 序列匹配。

### 5.3 点击单词 → 详情卡片

单击高亮词，底部弹出卡片：

```
┌───────────────────────────────────────┐
│ negotiate  /nɪˈɡoʊʃieɪt/   🔊        │
│ 不熟 5 次 · 练过 3 篇                  │
├───────────────────────────────────────┤
│ 文中义：谈判、协商                      │
│ 线索类型：因果情境                      │
│ 线索说明：前面说 both sides，后面说      │
│          walked away，可推知是"谈"      │
├───────────────────────────────────────┤
│ 你的标注：v. 谈判                       │
│ 原句（阅读模块）：The two sides refused │
│          to negotiate.                 │
│ 文中句子：Because the negotiations     │
│          collapsed, both sides...      │
├───────────────────────────────────────┤
│  [ 还不熟 ]      [ 认识了 ]             │
└───────────────────────────────────────┘
```

### 5.4 标记与计数回写（核心）

```ts
async function markWord(wordId: string, articleId: string, action: MarkResult) {
  const w = await getWord(wordId);

  if (action === 'unknown') {
    w.unknownCount  += 1;      // ★ 不熟次数 +1（核心）
    w.knownStreak    = 0;      // 连续认识清零
    w.lastUnknownAt  = now;
    // status 保持 learning
  } else {  // 'known'
    w.knownStreak   += 1;
    // unknown_count 不变
    if (w.knownStreak >= CONFIG.MASTERED_STREAK) {
      w.status     = 'mastered';
      w.masteredAt = now;      // 移出待练池
    }
  }

  w.lastExposedAt = now;
  await saveWord(w);
  await saveMark(articleId, wordId, action);   // context_article_words.marked_result
  await logEvent(wordId, action, articleId);   // word_events
}
```

**曝光计数规则**

- 文章**生成成功时**即对全部目标词 `exposure_count += 1`，并写 `last_exposed_at = now`。
- 理由：用户是主动点击生成的，不存在刷量问题，实现最简单，且能保证"间隔重复"逻辑有数据可用。
- 若后续发现数据失真，可改为「阅读完成或停留 ≥30 秒才计」，把这一行挪到 `/finish` 接口即可。

**未标记的词**：仅计曝光，`known_streak` 不变（不做任何猜测性推断）。

**文末批量操作**

- 「全部认识了」：对未标记的词批量执行 `known` 逻辑。
- 「完成」：标记 `context_articles.status = 'finished'`，写入 `finished_at`，弹出本篇小结（新学 X 词 / 标记不熟 Y 词 / 掌握 Z 词），并提供「再来一篇」按钮。

---

## 6. API 设计

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/words?pool=practice` | 待练池（`status=learning AND unknown_count>=1`），按 `priorityScore` 排序，返回含 score |
| `POST` | `/api/context/pick-words` | body `{ count?, wordIds? }` → Stage 1，返回 `{ scene, words[], reason }` |
| `POST` | `/api/context/generate` | body `{ wordIds, wordCount, genre, level, scene?, outputMode }` → Stage 2 + 校验 + 重试，返回 `ContextArticle` |
| `GET` | `/api/context/articles/:id` | 文章详情 + 每个目标词的 `firstSentence / clueType / senseZh / markedResult` |
| `POST` | `/api/context/articles/:id/mark` | body `{ wordId, action: 'unknown' \| 'known' }` |
| `POST` | `/api/context/articles/:id/mark-all-known` | 批量标记未标记词为「认识了」 |
| `POST` | `/api/context/articles/:id/finish` | 结束阅读，返回本篇小结 |
| `GET` | `/api/context/articles?limit=20` | 历史文章列表 |
| `GET` | `/api/stats/context` | 累计练习篇数、掌握词数、顽固词 Top10 |

---

## 7. 边界情况处理

| 场景 | 处理 |
|---|---|
| 待练词 < 5 个 | 不生成，引导去阅读模块划词，或提供词表导入入口 |
| 待练词 5–9 个 | 正常生成，顶部提示"可用生词不足 10 个，已用 N 个生成" |
| LLM 无法把 10 个词归入同一场景 | 降级为 5–9 个词；仍不行则拆成两篇、每篇 5 个 |
| 目标词为短语 | 走 `countPhrase` 完整串匹配，**不做词形还原** |
| 一词多义 | 把 `required_sense` 传给 LLM，若用户未指定则用 `user_gloss`；卡片中同时展示"文中义"与"你的标注"，不一致时提示用户 |
| 生成失败 / JSON 解析失败 | 重试 2 次，仍失败返回友好错误 + 「换一批词」按钮 |
| 同参数重复生成 | 命中缓存直接返回旧文章，并提供「强制重新生成」 |
| 某词连续标记「还不熟」≥5 次 | 打上 `stubborn` 标记（V2：单独出"顽固词专练"，每篇只放 3–5 个，配更强线索） |
| 用户已 mastered 全部生词 | 展示成就页 + 引导继续阅读模块积累新生词 |
| 长文性能 | `content_en` 切 token 后做一次索引（Map<lemma, number[]>），避免每次渲染重新扫描 |

---

## 8. 开发分期

**V1（先跑通闭环，必做）**
1. 生词表改造：新增 `unknown_count / exposure_count / known_streak / status`，入库默认 `unknown_count = 1`
2. 待练池筛选 + `priorityScore` 排序
3. LLM Stage 1 选词 + Stage 2 生成（默认 10 词 / 500 词 / story / B1）
4. 词形还原校验 + 重试 2 次
5. 阅读页高亮 + 点击卡片 + 「还不熟 / 认识了」
6. 计数回写 + 掌握状态流转

**V2**
- 体裁 / 难度 / 字数可调；手动选词；批量标记；本篇小结页
- 间隔重复调度（1 天 / 3 天 / 7 天 / 15 天）；顽固词专练
- 掌握曲线可视化；历史文章回看

**V3**
- 词表批量导入（四六级 / 考研 / 雅思核心词）；发音（TTS）；朗读跟读
- 错词强化（把标记不熟的词单独出填空题）；词库共享

---

## 9. 验收标准

| # | Given | When | Then |
|---|---|---|---|
| 1 | 生词 A 的 `unknown_count = 5`，生词 B = 1，二者均未曝光过 | 触发自动选词 | A 排在 B 前面 |
| 2 | 生词 C 刚在 1 天前进入过文章 | 再次触发选词 | C 被 `W_RECENT_PEN` 降权，不在 Top 10 |
| 3 | 生词 D 连续 5 次被标记「认识了」 | 第 3 次标记后 | `status = mastered`，从 `/api/words?pool=practice` 中消失 |
| 4 | 生词 E 已 mastered，又被标记「还不熟」 | 标记后 | `status = learning`，`unknown_count += 1`，`known_streak = 0`（V2 回炉逻辑） |
| 5 | 生成文章后校验发现 2 个词缺失 | 自动重试 | 重试文章中该 2 词已出现；仍缺失则 UI 明确提示 |
| 6 | 生词原形为 `go`，文中写作 `went` | 校验 | 判定为命中，不计入 missing |
| 7 | 目标词为短语 `look forward to`，文中写作 `looking forward to` | 校验 | 判定为缺失（短语不还原词形），进入重试 |
| 8 | 点击文章中的高亮词 | 弹出卡片 | 展示发音、文中义、用户标注义、阅读模块原句、线索说明 |
| 9 | 点击「还不熟」 | 回写 | `unknown_count += 1`，`known_streak = 0`，本篇该词高亮加粗 |
| 10 | 待练池只有 3 个词 | 触发生成 | 不生成，提示"至少 5 个词才能生成"，引导去阅读模块 |

---

## 10. 给 AI 写代码的执行提示

1. 先建数据模型与状态机，再写生成逻辑，最后写 UI——**计数口径一旦写错，整个模块的推荐质量就废了**。
2. `unknown_count` 初始值必须是 **1**，不是 0。
3. 词形还原校验不能省，否则会出现"目标词根本没出现"的尴尬文章。
4. Stage 1（选词 + 定场景）不能合并进 Stage 2，否则主题会散。
5. 高亮渲染不要用正则替换 HTML，用 token 数组渲染。
6. `CONFIG` 里的所有权重、阈值集中在一个常量文件中，便于后续调参。
