# 生词库 · 功能描述文档

> 当前规模：**68 个生词**
> 代码位置：`js/vocab.js`（核心）+ `js/select-popup.js`（划词弹窗）+ `js/eread.js`（精读提取）
> 一句话：**生词库是所有"不熟的词"的中转站与精加工车间**——从各处收进来，在这里做刻意练习（解释 → 造句 → 点评 → 入库），再分发到场景阅读、听写、表达库去复练。

---

## 1. 定位

```
【收】阅读/精读/场景阅读 划词 → 生词库
                              ↓
                    【精加工车间】（本模块）
                    解释 → 造句 → AI 点评 → 存学习库
                              ↓
【发】场景阅读（语境复练）· 听写（拼写导出）· 表达库（SuperMemo）
```

生词库不是终点，是**枢纽**：词先进来打磨，再流向三个下游各练一遍。

---

## 2. 数据模型（生词条目字段）

| 字段 | 说明 |
|---|---|
| `id` / `word` / `display` | 主键 / 小写原形（去重键）/ 展示形式（短语存完整串） |
| `sentence` | 出处原句（划词时的上下文） |
| `phonetic` | 音标（AI 解释时填入） |
| `explain` | 释义（词性 + 中文 + 新例句，AI 自动填或手写） |
| `example` | 用户造的句子（刻意练习核心） |
| `fb` | AI 对造句的点评数组（可累积多轮） |
| `saved` | 是否已存入表达库 |
| `added` | 入库日期 |
| `unknownCount` / `exposureCount` / `knownStreak` / `status` | 场景阅读计数：不熟次数 / 练过几篇 / 连续认识 / learning·mastered |
| `exportedTypes` / `exportRound` / `exportedAt` / `dictationEnabled` | 听写导出状态 |
| `ctxSeenAt` | 场景阅读中当天点开过解释（计入统计） |

---

## 3. 三个入库入口

| 入口 | 位置 | 说明 |
|---|---|---|
| **阅读划词** | 阅读/精读/场景阅读 任务里 | 划选单词或短语 → 弹窗出 AI 解释（音标+释义+例句）→ 一键加入，自动带出处原句 |
| **精读 AI 提取** | 精读页「AI 提取生词词组」 | 批量提取，释义直接写入（跳过「待解释」），逐条或一键全部存入 |
| **场景阅读点开** | 场景阅读正文 | 点开目标词看解释即记录 `ctxSeenAt`（计入当天新学统计） |

去重：按 `word`（小写原形）匹配，已存在则更新出处句子；**已毕业（mastered）的词重新加入会自动回炉**——状态改回 learning、不熟次数+1、连续认识清零。

---

## 4. 精加工车间（核心刻意练习流程）

每个生词四步，页面顶部有进度总览 + 每个词带状态标签：

| 步骤 | 操作 | 状态标签 |
|---|---|---|
| 1. AI 解释 | 点「AI 解释（结合原句）」→ 音标 + 词性释义 + 新例句 | 🔴 待解释 |
| 2. 听发音 | 点 🔊（浏览器 TTS，离线可用） | — |
| 3. 造句 | 「请造句：」框里用自己的话造一句 | 🟠 待造句 |
| 4. AI 点评 | 点「AI 点评造句」深度复盘，可累积多轮 | — |
| 5. 入库 | 「存入学习库」→ 进表达库，进 SuperMemo | ⚪ 未入库 / ✅ 已入库 |

**筛选**：全部 / 待解释 / 待造句 / 未入库，一眼看清还差哪步。

---

## 5. 下游联动（词打磨完去哪）

| 下游 | 干什么 | 用什么字段 |
|---|---|---|
| **场景阅读** | 把不熟次数高的词编进短文复练 | `unknownCount`（排序主项）/ `knownStreak`（满 5 毕业）/ `status` |
| **听写** | 导出拼写挖空题给 SuperMemo | `display` / `explain` / `phonetic` / `sentence`；`exportedTypes` 去重 |
| **表达库** | 「存入学习库」转成 word 卡导出 | `wordId` 引用（不拷贝内容），`saved` 标记 |

**毕业词回炉**：场景阅读里满 5 次「认识了」毕业（`mastered`）移出待练池；若之后又忘了重新划词加入，自动回到 `learning` 继续练。

---

## 6. 统计联动

- 当天划词收进来的：单词计入「新学单词」、短语计入「新学短语」
- 场景阅读当天点开看过解释的词：也计入当天新学
- 「近 14 天新学内容」柱状图按单词/短语堆叠展示

---

## 7. 与其它模块的边界

| 模块 | 和生词库的关系 |
|---|---|
| 阅读/精读 | **只管划词收进来**，精加工都在生词库做（"阅读只管阅读"） |
| 场景阅读 | 生词库的**复读机**——词在这里反复在语境里出现 |
| 听写 | 生词库的**拼写出口**——导出挖空题给 SuperMemo |
| 表达库 | 生词库的**归档出口**——「存入学习库」后可导出 SuperMemo |

**一句话**：生词库负责"把一个生词真正学会"，其它模块负责"在更多场景里反复遇见它"。

---

*生成时间：2026-08-31 · 当前生词数：68*
